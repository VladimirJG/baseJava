package com.basejava.webapp.storage;


class ArrayStorageTest extends AbstractArrayStorageTest{

    ArrayStorageTest(Storage storage) {
        super(storage);
    }
    ArrayStorage arrayStorage = new ArrayStorage();
}